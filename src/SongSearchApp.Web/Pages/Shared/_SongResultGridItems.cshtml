@model IReadOnlyList<SongSearchApp.Models.Application.SongData>

@foreach (var song in Model)
{
    <div class="col-12 col-md-6">
        <div class="card h-100">
            <div class="card-body d-flex gap-3">
                <partial name="_SongResultCard" model="song" />
            </div>
        </div>
    </div>
}
